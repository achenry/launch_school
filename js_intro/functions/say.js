function say(words = "hello") {
  // words is a parameter, a local variable defined within function body
  console.log(words + "!");
}

say("hello"); // 'hello' is passed as an argument to the function invocation/call to initialize the parameter words
say("hi");
say("how do you do");
say("Quite all right");
say();